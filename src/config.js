// Global site configuration
export const config = {
  // Google Tag Manager container ID (e.g. 'GTM-XXXXXX')
  // Set to empty string or null to disable GTM tracking
  gtmContainerId: 'GTM-NMKKB526', 
};
